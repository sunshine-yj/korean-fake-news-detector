# ── 딥러닝 · 트랜스포머 ──
torch>=2.0
transformers>=4.40
tokenizers>=0.19
datasets>=2.18
sentencepiece>=0.2          # KPF-BERT / MarianMT 토크나이저

# ── 머신러닝 · 앙상블 ──
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
catboost>=1.2
joblib>=1.3

# ── 데이터 처리 ──
numpy>=1.26
pandas>=2.2
openpyxl>=3.1               # .xlsx 읽기
tqdm>=4.66

# ── 데이터베이스 ──
PyMySQL>=1.1
SQLAlchemy>=2.0

# ── 서빙 ──
Flask>=3.0
flask-cors>=4.0

# ── 유틸 ──
python-dotenv>=1.0
PyYAML>=6.0

# ── 시각화 (실험 분석용) ──
matplotlib>=3.8
seaborn>=0.13

# 참고: 정확한 버전 고정이 필요하면 아래로 생성한다.
#   pip freeze > requirements.lock.txt
# RTX 5070 등 최신 GPU 는 CUDA 버전에 맞는 torch 휠이 필요하므로
# https://pytorch.org/get-started/locally/ 에서 확인할 것.
